editing
- local modifications storage
  - should it be in indexed db for easier migrations?
- song notes/stickies

song edit UI
- larger single popover for song settings
    - key,
    - capo (if enabled)
    - bpm
    - pads on/off for this song
        - pad sound 
    - click on/off for this song
        - click pattern

preferences
  - custom section visibility defaults

storage/sync
- edit chordpro/variants
- background syncing (periodic, and fallbacks)

live mode
 - button on setlist
 - fullscreen
 - keep screen alive
 - simple standalone sharing
 - webrtc sync

tests
 - component tests (using test page?)

ccli import
 - don't go back to import page (history)
 - adding the same song twice to set lists
 - try importing in original key, but record the key that it was set to in the songselect page, and add to setlist in that key
  - handle numbers on import (need to get an original key)
 - make it reuse open windows for import (maybe need to get the default app, not opened via import, to have the tag tag?)

sharing
- temporary sharing
- email media team

player
 - click stuttering start
 - survive pageloads? -maybe migrate to SPA

UI
- device type fonts (mobile, tablet, desktop) - partially done.
- autoscaling font
- logo

library
 - ccli report nag
